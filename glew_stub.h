#include <stdlib.h>
#include <GL/glew.h>
extern void go_glPrimitiveRestartIndex(GLuint index);
extern void go_glDrawRangeElements(GLenum mode, GLuint start, GLuint end, GLsizei count, GLenum type, const GLvoid *indices);
